import React from 'react';
import {newPage} from "@/components/services/api.service";

const Pagination = () => {
    let page = newPage;
    return (
        <div>

            </div>
    );
};

export default Pagination;